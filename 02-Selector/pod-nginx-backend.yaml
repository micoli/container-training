---

apiVersion: v1
kind: Pod
metadata:
  name: nginx-backend
  labels:
    environment : dev
    type: application
    app: backend
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80